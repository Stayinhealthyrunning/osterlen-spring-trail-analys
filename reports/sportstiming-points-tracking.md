# Sportstiming points/tracking inventory

Public `/app/points` and `/app/tracking` views inspected without bypassing access controls.

## 2018 (event 4950)

### points
- HTTP: None
- HTML: 0 bytes
- Selects: 0
- Relevant links: 0

### tracking
- HTTP: 200
- HTML: 42516 bytes
- Selects: 1
- Relevant links: 10
- Candidate text:
  - $('.timepausespan').hide(); //$('#timeplaypause').css("background-image", "url('/Results/Tracking/timeplay.png')"); } else { $('.timeplayspan').hide(); $('.timepausespan').show(); //$('#timeplaypause').css("background-image", "url('/Resu
  - lts/Tracking/timepause.png')"); } }); // GRAPH if (viewSportsGraph) { graph = new sportsgraph.circular(document.getElementById('chart_div'), options, gr
  - e) { if (request.term.length >= 3 || $.isNumeric(request.term)) { $.ajax({ url: "/Results/Tracking.aspx/SearchParticipant", dataType: "json", data: "{ 'rallyID': " + eventID + ", 'roundID': " + raceID + ", 'query': '" + request.term + "'
  - lor: #212121; text-align: left; font-size: 11px; } .acInputMini { width: 250px; } Rute og tracking Event Deltagere Resultater Rute og tracking Event Deltagere Resultater Rute og tracking Tracking Österlen Spring Trail 2018 Österlen Spring

## 2019 (event 5719)

### points
- HTTP: 200
- HTML: 183541 bytes
- Selects: 2
- Relevant links: 11
- Candidate text:
  - e = \'st\'; <\/script>'; Gripenserien Poäng Event Deltagarna Resultat Tävlingskarta (Live tracking) Gripenserien Poäng Event Deltagarna Resultat Tävlingskarta (Live tracking) Tracking Gripenserien Poäng Byt tävling Österlen Spring Trail Ö

### tracking
- HTTP: 200
- HTML: 41282 bytes
- Selects: 1
- Relevant links: 11
- Candidate text:
  - width: 250px; } Rute og tracking Event Deltagere Resultater Rute og tracking Gripenserien Point Event Deltagere Resultater Rute og tracking Tracking Gripenserien Point Österlen Spring Trail 2019 Österlen Spring Trail Österlen Spring Tr
  - $('.timepausespan').hide(); //$('#timeplaypause').css("background-image", "url('/Results/Tracking/timeplay.png')"); } else { $('.timeplayspan').hide(); $('.timepausespan').show(); //$('#timeplaypause').css("background-image", "url('/Resu
  - lts/Tracking/timepause.png')"); } }); // GRAPH if (viewSportsGraph) { graph = new sportsgraph.circular(document.getElementById('chart_div'), options, gr
  - e) { if (request.term.length >= 3 || $.isNumeric(request.term)) { $.ajax({ url: "/Results/Tracking.aspx/SearchParticipant", dataType: "json", data: "{ 'rallyID': " + eventID + ", 'roundID': " + raceID + ", 'query': '" + request.term + "'
  - lor: #212121; text-align: left; font-size: 11px; } .acInputMini { width: 250px; } Rute og tracking Event Deltagere Resultater Rute og tracking Gripenserien Point Event Deltagere Resultater Rute og tracking Tracking Gripenserien Point Öste

## 2022 (event 9587)

### points
- HTTP: None
- HTML: 0 bytes
- Selects: 0
- Relevant links: 0

### tracking
- HTTP: 200
- HTML: 244368 bytes
- Selects: 1
- Relevant links: 10
- Candidate text:
  - { position: new google.maps.LatLng(55.55669, 14.3547), map: map, icon: '/Results/Tracking/start.png', zIndex: 5}); var icon2 = new google.maps.Marker({ position: new google.maps.LatLng(55.71733, 13.96054), map: map, icon: '/Results/Tra
  - position: new google.maps.LatLng(55.71733, 13.96054), map: map, icon: '/Results/Tracking/finish.png', zIndex: 6}); } var isDraggable = true; var OPACITY_MAX_PIXELS = 57; // Width of opacity control image var runnerInfo = { "runners": [
  - ltitudeMeters": ["18"] }]; var options = { "markings": [ { location: 5000, url: "/Results/Tracking/km5signbig.png", yPixelOffset: 7 }, { location: 10000, url: "/Results/Tracking/km10signbig.png", yPixelOffset: 7 }, { location: 15000, url:
  - "/Results/Tracking/km15signbig.png", yPixelOffset: 7 }, { location: 20000, url: "/Results/Tracking/km20signbig.png", yPixelOffset: 7 }, { location: 25000, url
  - : "/Results/Tracking/km25signbig.png", yPixelOffset: 7 }, { location: 30000, url: "/Results/Tracking/km30signbig.png", yPixelOffset: 7 }, { location: 35000, url
  - : "/Results/Tracking/km35signbig.png", yPixelOffset: 7 }, { location: 40000, url: "/Results/Tracking/km40signbig.png", yPixelOffset: 7 }, { location: 45000, url
  - : "/Results/Tracking/km45signbig.png", yPixelOffset: 7 }, { location: 50000, url: "/Results/Tracking/km50signbig.png", yPixelOffset: 7 }, { location: 55000, url
  - : "/Results/Tracking/km55signbig.png", yPixelOffset: 7 }, { location: 60000, url: "/Results/Tracking/km60signbig.png", yPixelOffset: 7 }, {location: 1000, size:
  - s.Marker({ position: new google.maps.LatLng(55.55669, 14.3547), map: map, icon: '/Results/Tracking/start.png', zIndex: 5}); var icon2 = new google.maps.Marker({ position: new google.maps.LatLng(55.71733, 13.96054), map: map, icon: '/Resul
  - ts/Tracking/finish.png', zIndex: 6}); } var isDraggable = true; var OPACITY_MAX_PIXELS = 57; // Width of opacity control image var runnerInfo = { "runn

## 2023 (event 11274)

### points
- HTTP: None
- HTML: 0 bytes
- Selects: 0
- Relevant links: 0

### tracking
- HTTP: 200
- HTML: 37999 bytes
- Selects: 1
- Relevant links: 10
- Candidate text:
  - $('.timepausespan').hide(); //$('#timeplaypause').css("background-image", "url('/Results/Tracking/timeplay.png')"); } else { $('.timeplayspan').hide(); $('.timepausespan').show(); //$('#timeplaypause').css("background-image", "url('/Resu
  - lts/Tracking/timepause.png')"); } }); // GRAPH if (viewSportsGraph) { graph = new sportsgraph.circular(document.getElementById('chart_div'), options, gr
  - e) { if (request.term.length >= 3 || $.isNumeric(request.term)) { $.ajax({ url: "/Results/Tracking.aspx/SearchParticipant", dataType: "json", data: "{ 'rallyID': " + eventID + ", 'roundID': " + raceID + ", 'query': '" + request.term + "'
  - tialize('ctl00$ctl00$ScriptManager1', 'aspnetForm', [], [], [], 90, 'ctl00$ctl00'); //]]> Tracking Österlen Spring Trail 2023 Afspil rute Pause //<![CDATA[ Sys.Application.add_init(function() { $create(Sys.UI._UpdateProgress, {"associated

## 2024 (event 12349)

### points
- HTTP: None
- HTML: 0 bytes
- Selects: 0
- Relevant links: 0

### tracking
- HTTP: 200
- HTML: 147375 bytes
- Selects: 2
- Relevant links: 10
- Candidate text:
  - 2019 Österlen Spring Trail 2018 Österlen Spring Trail 2017 Österlen Spring Trail 2016 /* Point of interest pop up styles todo */ .leaflet-popup-content-wrapper { border-radius: 8px !important; background: #fff !important; padding: 0px
  - OverlayLayer instance /* Misc */ var isAarhusMotion = false; var isDeveloper = false; // Point of interest, such as start, finish, hotspot etc. class PointOfInterest { constructor(id, lng, lat, icon, name, image, text, link) { this.id
  - this.layergroup.removeLayer(entry.marker); } } } } getPositionByDistance(distance) { var point = null; var pointTotalDistance = 0; var prevPoint = null; var prevPointTotalDistance = 0; var pointsOnRoute = this.polyLineArray; // Comput
  - oking for for (var i = 0; i < pointsOnRoute.length; i++) { // Store reference to previous point, if any prevPoint = point; prevPointTotalDistance = pointTotalDistance; // Compute how far we got on this point point = pointsOnRoute[i]; v
  - ar distanceSinceLastPoint = 0; if (prevPoint !== null && point !== null) { distanceSinceLastPoint = prevPoint.distanceTo(point); pointTotalDistance += distanceSinceLastPoint; } if (pointTotalDistance >=
  - ak; } } // If we have an exact match (theoretical due to decimal points, unless the start point), return the exact match point if (pointTotalDistance === distance) return point; // If only one (or none), return the last point reached i
  - f (prevPoint === null || point === null) return point; // When we have two points A and B, interpolate a position between them, in respect to how long far we travelled var distanceAfterFirstPoi
  - nt = distance - prevPointTotalDistance; // Distance after point A we have left to go towards B var distanceBetweenPoints = pointTotalDistance - prevPointTotalDistance; // Distance from point A to B // Ra
  - ) var ratio = distanceAfterFirstPoint / distanceBetweenPoints; var lat = prevPoint.lat + (point.lat - prevPoint.lat) * ratio; var lng = prevPoint.lng + (point.lng - prevPoint.lng) * ratio; return new L.LatLng(lat, lng); } //Returns the
  - tPosition, this.route_id); } } } else if (this.status_isfinished) { // Jump to the finish point (last point on the route) // We do this instead of computing the distance to always match exactly the finish point, even if the route dista

## 2025 (event 15015)

### points
- HTTP: None
- HTML: 0 bytes
- Selects: 0
- Relevant links: 0

### tracking
- HTTP: 200
- HTML: 147375 bytes
- Selects: 2
- Relevant links: 10
- Candidate text:
  - 2019 Österlen Spring Trail 2018 Österlen Spring Trail 2017 Österlen Spring Trail 2016 /* Point of interest pop up styles todo */ .leaflet-popup-content-wrapper { border-radius: 8px !important; background: #fff !important; padding: 0px
  - OverlayLayer instance /* Misc */ var isAarhusMotion = false; var isDeveloper = false; // Point of interest, such as start, finish, hotspot etc. class PointOfInterest { constructor(id, lng, lat, icon, name, image, text, link) { this.id
  - this.layergroup.removeLayer(entry.marker); } } } } getPositionByDistance(distance) { var point = null; var pointTotalDistance = 0; var prevPoint = null; var prevPointTotalDistance = 0; var pointsOnRoute = this.polyLineArray; // Comput
  - oking for for (var i = 0; i < pointsOnRoute.length; i++) { // Store reference to previous point, if any prevPoint = point; prevPointTotalDistance = pointTotalDistance; // Compute how far we got on this point point = pointsOnRoute[i]; v
  - ar distanceSinceLastPoint = 0; if (prevPoint !== null && point !== null) { distanceSinceLastPoint = prevPoint.distanceTo(point); pointTotalDistance += distanceSinceLastPoint; } if (pointTotalDistance >=
  - ak; } } // If we have an exact match (theoretical due to decimal points, unless the start point), return the exact match point if (pointTotalDistance === distance) return point; // If only one (or none), return the last point reached i
  - f (prevPoint === null || point === null) return point; // When we have two points A and B, interpolate a position between them, in respect to how long far we travelled var distanceAfterFirstPoi
  - nt = distance - prevPointTotalDistance; // Distance after point A we have left to go towards B var distanceBetweenPoints = pointTotalDistance - prevPointTotalDistance; // Distance from point A to B // Ra
  - ) var ratio = distanceAfterFirstPoint / distanceBetweenPoints; var lat = prevPoint.lat + (point.lat - prevPoint.lat) * ratio; var lng = prevPoint.lng + (point.lng - prevPoint.lng) * ratio; return new L.LatLng(lat, lng); } //Returns the
  - tPosition, this.route_id); } } } else if (this.status_isfinished) { // Jump to the finish point (last point on the route) // We do this instead of computing the distance to always match exactly the finish point, even if the route dista

## 2026 (event 16880)

### points
- HTTP: None
- HTML: 0 bytes
- Selects: 0
- Relevant links: 0

### tracking
- HTTP: 200
- HTML: 147371 bytes
- Selects: 2
- Relevant links: 10
- Candidate text:
  - 2019 Österlen Spring Trail 2018 Österlen Spring Trail 2017 Österlen Spring Trail 2016 /* Point of interest pop up styles todo */ .leaflet-popup-content-wrapper { border-radius: 8px !important; background: #fff !important; padding: 0px
  - OverlayLayer instance /* Misc */ var isAarhusMotion = false; var isDeveloper = false; // Point of interest, such as start, finish, hotspot etc. class PointOfInterest { constructor(id, lng, lat, icon, name, image, text, link) { this.id
  - this.layergroup.removeLayer(entry.marker); } } } } getPositionByDistance(distance) { var point = null; var pointTotalDistance = 0; var prevPoint = null; var prevPointTotalDistance = 0; var pointsOnRoute = this.polyLineArray; // Comput
  - oking for for (var i = 0; i < pointsOnRoute.length; i++) { // Store reference to previous point, if any prevPoint = point; prevPointTotalDistance = pointTotalDistance; // Compute how far we got on this point point = pointsOnRoute[i]; v
  - ar distanceSinceLastPoint = 0; if (prevPoint !== null && point !== null) { distanceSinceLastPoint = prevPoint.distanceTo(point); pointTotalDistance += distanceSinceLastPoint; } if (pointTotalDistance >=
  - ak; } } // If we have an exact match (theoretical due to decimal points, unless the start point), return the exact match point if (pointTotalDistance === distance) return point; // If only one (or none), return the last point reached i
  - f (prevPoint === null || point === null) return point; // When we have two points A and B, interpolate a position between them, in respect to how long far we travelled var distanceAfterFirstPoi
  - nt = distance - prevPointTotalDistance; // Distance after point A we have left to go towards B var distanceBetweenPoints = pointTotalDistance - prevPointTotalDistance; // Distance from point A to B // Ra
  - ) var ratio = distanceAfterFirstPoint / distanceBetweenPoints; var lat = prevPoint.lat + (point.lat - prevPoint.lat) * ratio; var lng = prevPoint.lng + (point.lng - prevPoint.lng) * ratio; return new L.LatLng(lat, lng); } //Returns the
  - tPosition, this.route_id); } } } else if (this.status_isfinished) { // Jump to the finish point (last point on the route) // We do this instead of computing the distance to always match exactly the finish point, even if the route dista

